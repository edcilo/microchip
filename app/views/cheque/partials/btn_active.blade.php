@if( p(15) )
    <a class="btn-green btn-active" title="Regresar cheque a la lista" href="#" data-id="{{ $cheque->id }}" data-name="{{ $cheque->folio }}">
        <i class="fa fa-arrow-up"></i>
    </a>
@endif
