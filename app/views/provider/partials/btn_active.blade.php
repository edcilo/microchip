@if( p(39) )

    <a class="btn-green btn-active" title="Regresar proveedor a la lista" href="#" data-id="{{ $provider->id }}" data-name="{{ $provider->name }}">
        <i class="fa fa-arrow-up"></i>
    </a>

    @endif
